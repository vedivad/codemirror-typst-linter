{
  "name": "typst-web",
  "private": true,
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "build": "bun run --cwd packages/typst-web-service build && bun run --cwd packages/codemirror-typst build",
    "dev": "bun run --filter 'packages/*' dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
