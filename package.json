{
  "name": "typst-web",
  "private": true,
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "build": "bun run --filter 'packages/*' build",
    "dev": "bun run --filter 'packages/*' dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
